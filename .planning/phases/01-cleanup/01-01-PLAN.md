---
phase: 01-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ClaudeUsageWidget/ClaudeUsageWidgetProvider/ClaudeUsageWidgetProvider.csproj
  - ClaudeUsageWidget/ClaudeUsageWidgetProvider/Program.cs
  - ClaudeUsageWidget/ClaudeUsageWidgetProvider/WidgetProvider.cs
  - ClaudeUsageWidget/ClaudeUsageWidgetProvider/FactoryHelper.cs
  - ClaudeUsageWidget/ClaudeUsageWidgetProvider/ByparrClient.cs
  - ClaudeUsageWidget/ClaudeUsageWidgetProvider/Package.appxmanifest
  - ClaudeUsageWidget/ClaudeUsageWidgetProvider/Templates/
  - ClaudeUsageWidget/ClaudeUsageWidgetProvider/Assets/
  - ClaudeUsageWidget/ClaudeUsageWidgetProvider/ProviderAssets/
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03
  - CLEAN-04
  - LIFE-02

must_haves:
  truths:
    - "dotnet build ClaudeUsageWidget/ClaudeUsageWidgetProvider/ClaudeUsageWidgetProvider.csproj projde bez chyb"
    - "ClaudeApiClient.cs a CredentialStore.cs existují beze změny"
    - "WidgetProvider.cs, FactoryHelper.cs, ByparrClient.cs neexistují"
    - "V .csproj není reference na Microsoft.WindowsAppSDK ani Microsoft.Windows.SDK.BuildTools"
    - "V .csproj je <UseWPF>true</UseWPF>"
    - "Výsledný .exe lze spustit přímo bez MSIX instalace"
  artifacts:
    - path: "ClaudeUsageWidget/ClaudeUsageWidgetProvider/ClaudeUsageWidgetProvider.csproj"
      provides: "WPF projekt bez MSIX závislostí"
      contains: "UseWPF"
    - path: "ClaudeUsageWidget/ClaudeUsageWidgetProvider/Program.cs"
      provides: "WPF Application startup"
      contains: "Application"
    - path: "ClaudeUsageWidget/ClaudeUsageWidgetProvider/ClaudeApiClient.cs"
      provides: "Zachovaný API client"
    - path: "ClaudeUsageWidget/ClaudeUsageWidgetProvider/CredentialStore.cs"
      provides: "Zachovaný credential store"
  key_links:
    - from: "Program.cs"
      to: "ClaudeApiClient.cs"
      via: "WPF Application class"
      pattern: "ClaudeApiClient"
---

<objective>
Přeměnit existující MSIX Widget provider projekt na čisté WPF exe.

Purpose: Odstranit veškerý MSIX/Widget provider balast a připravit základ pro Phase 2 (UI).
Output: Kompilující WPF projekt s ClaudeApiClient.cs a CredentialStore.cs, bez Widget provider kódu.
</objective>

<execution_context>
@/home/zrny/.claude/get-shit-done/workflows/execute-plan.md
@/home/zrny/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cleanup/01-CONTEXT.md

<interfaces>
<!-- Existující soubory k zachování beze změny -->

ClaudeUsageWidget/ClaudeUsageWidgetProvider/ClaudeApiClient.cs — zachovat celý:
- class ClaudeApiClient: HTTP client pro api.anthropic.com/v1/messages
- class UsageData: DTO s FiveHourUtilization, SevenDayUtilization, reset timestamps
- Používá OAuthCredential z CredentialStore.cs

ClaudeUsageWidget/ClaudeUsageWidgetProvider/CredentialStore.cs — zachovat celý:
- class CredentialStore: čte ~/.claude/.credentials.json z Windows i WSL cest
- class OAuthCredential: token model s IsExpired

<!-- Soubory ke smazání -->
- WidgetProvider.cs (COM Widget provider, celý soubor)
- FactoryHelper.cs (COM factory helper, celý soubor)
- ByparrClient.cs (prázdný stub, celý soubor)
- Templates/ adresář (JSON šablony pro Windows Widget)
- Assets/ adresář (MSIX loga a ikony)
- ProviderAssets/ adresář
- Package.appxmanifest

<!-- FormatResetTime() z WidgetProvider.cs — užitečná helper metoda -->
<!-- Přesunout do Program.cs nebo budoucí utility class pro Phase 2 -->
static string FormatResetTime(DateTimeOffset resetTime)
{
    var remaining = resetTime - DateTimeOffset.UtcNow;
    if (remaining <= TimeSpan.Zero) return "now";
    if (remaining.TotalHours >= 1)
        return $"in {(int)remaining.TotalHours}h {remaining.Minutes}m";
    return $"in {remaining.Minutes}m";
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Přepsat .csproj a smazat Widget provider soubory</name>
  <files>
    ClaudeUsageWidget/ClaudeUsageWidgetProvider/ClaudeUsageWidgetProvider.csproj,
    ClaudeUsageWidget/ClaudeUsageWidgetProvider/WidgetProvider.cs,
    ClaudeUsageWidget/ClaudeUsageWidgetProvider/FactoryHelper.cs,
    ClaudeUsageWidget/ClaudeUsageWidgetProvider/ByparrClient.cs,
    ClaudeUsageWidget/ClaudeUsageWidgetProvider/Package.appxmanifest,
    ClaudeUsageWidget/ClaudeUsageWidgetProvider/Templates/ (smazat adresář),
    ClaudeUsageWidget/ClaudeUsageWidgetProvider/Assets/ (smazat adresář),
    ClaudeUsageWidget/ClaudeUsageWidgetProvider/ProviderAssets/ (smazat adresář)
  </files>
  <action>
1. Přepsat ClaudeUsageWidgetProvider.csproj na čistý WPF projekt:
   - Zachovat: OutputType=Exe, TargetFramework=net8.0-windows10.0.22621.0, Nullable=enable, ImplicitUsings=enable, RuntimeIdentifiers=win-x64;win-arm64, Platforms=x64;ARM64
   - Přidat: &lt;UseWPF&gt;true&lt;/UseWPF&gt;
   - Odstranit: EnableMsixTooling, WindowsPackageType, AppxPackageSigningEnabled, WindowsAppSDKSelfContained, PublishProfile
   - Odstranit PackageReference: Microsoft.WindowsAppSDK, Microsoft.Windows.SDK.BuildTools
   - Odstranit ItemGroup s Templates a ProviderAssets (soubory budou smazány)

2. Smazat soubory:
   - WidgetProvider.cs
   - FactoryHelper.cs
   - ByparrClient.cs
   - Package.appxmanifest

3. Smazat adresáře:
   - Templates/
   - Assets/
   - ProviderAssets/

Výsledný .csproj má POUZE: PropertyGroup s WPF properties, žádné PackageReference na AppSDK.
  </action>
  <verify>
    <automated>grep -L "WindowsAppSDK\|EnableMsixTooling\|WindowsPackageType" /mnt/c/Users/stepa/source/claude-usage-widget/ClaudeUsageWidget/ClaudeUsageWidgetProvider/ClaudeUsageWidgetProvider.csproj && grep -l "UseWPF" /mnt/c/Users/stepa/source/claude-usage-widget/ClaudeUsageWidget/ClaudeUsageWidgetProvider/ClaudeUsageWidgetProvider.csproj</automated>
  </verify>
  <done>
    .csproj neobsahuje žádnou referenci na MSIX/WindowsAppSDK, obsahuje UseWPF=true.
    Soubory WidgetProvider.cs, FactoryHelper.cs, ByparrClient.cs, Package.appxmanifest neexistují.
    Adresáře Templates/, Assets/, ProviderAssets/ neexistují.
  </done>
</task>

<task type="auto">
  <name>Task 2: Přepsat Program.cs na WPF startup a ověřit kompilaci</name>
  <files>
    ClaudeUsageWidget/ClaudeUsageWidgetProvider/Program.cs
  </files>
  <action>
Přepsat Program.cs jako minimální WPF Application startup:

```csharp
using System.Windows;

namespace ClaudeUsageWidgetProvider;

internal class App : Application
{
    [STAThread]
    static void Main(string[] args)
    {
        var app = new App();
        app.Run();
    }
}
```

Tento soubor je záměrně minimální — žádná logika, žádné okno (zatím). Phase 2 přidá MainWindow.
Přesunout FormatResetTime() helper z WidgetProvider.cs sem jako statickou metodu — bude potřeba v Phase 2:

```csharp
internal static class TimeFormatter
{
    internal static string FormatResetTime(DateTimeOffset resetTime)
    {
        var remaining = resetTime - DateTimeOffset.UtcNow;
        if (remaining <= TimeSpan.Zero) return "now";
        if (remaining.TotalHours >= 1)
            return $"in {(int)remaining.TotalHours}h {remaining.Minutes}m";
        return $"in {remaining.Minutes}m";
    }
}
```

TimeFormatter může být ve stejném souboru Program.cs nebo samostatný TimeFormatter.cs — dle uvážení.

Po zápisu spustit kompilaci:
```
cd /mnt/c/Users/stepa/source/claude-usage-widget/ClaudeUsageWidget/ClaudeUsageWidgetProvider
dotnet build --configuration Debug
```

Pokud kompilace selže kvůli chybějícím using nebo referenci, opravit. Cíl: nulové chyby, varování jsou přijatelná.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/stepa/source/claude-usage-widget/ClaudeUsageWidget/ClaudeUsageWidgetProvider && dotnet build --configuration Debug 2>&1 | tail -5</automated>
  </verify>
  <done>
    dotnet build skončí s "Build succeeded" a 0 chybami.
    Program.cs používá System.Windows.Application (ne COM/Widget provider API).
    ClaudeApiClient.cs a CredentialStore.cs jsou beze změny.
  </done>
</task>

</tasks>

<verification>
Po dokončení obou tasks:

1. `dotnet build` v adresáři projektu vrátí "Build succeeded" bez chyb
2. `ls ClaudeUsageWidget/ClaudeUsageWidgetProvider/` neobsahuje: WidgetProvider.cs, FactoryHelper.cs, ByparrClient.cs, Package.appxmanifest, Templates/, Assets/, ProviderAssets/
3. `grep UseWPF ClaudeUsageWidget/ClaudeUsageWidgetProvider/ClaudeUsageWidgetProvider.csproj` vrátí výsledek
4. `grep -r "WindowsAppSDK\|EnableMsixTooling" ClaudeUsageWidget/ClaudeUsageWidgetProvider/ClaudeUsageWidgetProvider.csproj` vrátí prázdný výstup
5. ClaudeApiClient.cs a CredentialStore.cs existují beze změny
</verification>

<success_criteria>
- `dotnet build` projde bez chyb
- Projekt neobsahuje MSIX/WindowsAppSDK reference ani Widget provider soubory
- ClaudeApiClient.cs a CredentialStore.cs jsou zachovány jako základ pro Phase 2 a 3
- Výsledné exe lze spustit přímo (bez MSIX instalace)
</success_criteria>

<output>
Po dokončení vytvořit `.planning/phases/01-cleanup/01-01-SUMMARY.md` se stručným souhrnem co bylo provedeno, zachovány soubory, smazány soubory, výsledek kompilace.
</output>
