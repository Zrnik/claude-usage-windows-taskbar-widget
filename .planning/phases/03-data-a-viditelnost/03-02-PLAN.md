---
phase: 03-data-a-viditelnost
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - ClaudeUsageWidget/MainWindow.xaml.cs
autonomous: true
requirements:
  - VIS-01
  - VIS-02

must_haves:
  truths:
    - "Při auto-hide taskbaru widget zmizí když taskbar zajede dolů"
    - "Widget se vrátí až taskbar dosáhne finální pozice nahoře"
    - "Při fullscreen aplikaci na stejném monitoru widget zmizí"
    - "Fullscreen na jiném monitoru widget neovlivní"
    - "Visibility timer se zastaví při zavření okna"
  artifacts:
    - path: "ClaudeUsageWidget/MainWindow.xaml.cs"
      provides: "DispatcherTimer _visibilityTimer, IsTaskbarVisible(), IsFullscreenOnMyMonitor(), CheckVisibility()"
      contains: "_visibilityTimer"
  key_links:
    - from: "Loaded event handler"
      to: "StartVisibilityTimer()"
      via: "volání v Loaded lambda"
      pattern: "StartVisibilityTimer"
    - from: "_visibilityTimer.Tick"
      to: "CheckVisibility()"
      via: "Tick event handler"
      pattern: "CheckVisibility"
    - from: "CheckVisibility()"
      to: "Visibility.Visible / Visibility.Hidden"
      via: "taskbarVisible && !fullscreen podmínka"
      pattern: "taskbarVisible && !fullscreen"
---

<objective>
Přidat visibility timer pro auto-hide taskbar a fullscreen detekci.

Purpose: Widget musí zmizet když taskbar zajede dolů (auto-hide) nebo když fullscreen aplikace pokryje monitor. Všechny Win32 importy jsou z Phase 2 — přidáváme jen logiku.
Output: Nový 500ms DispatcherTimer v MainWindow.xaml.cs se dvěma detekčními metodami.
</objective>

<execution_context>
@/home/zrny/.claude/get-shit-done/workflows/execute-plan.md
@/home/zrny/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-data-a-viditelnost/03-01-SUMMARY.md
@ClaudeUsageWidget/MainWindow.xaml.cs

<interfaces>
<!-- Existující Win32 importy v MainWindow.xaml.cs — všechny použitelné pro VIS logiku -->

Již importováno (NEIMPORTOVAT ZNOVU):
```csharp
[DllImport("shell32.dll")]
private static extern uint SHAppBarMessage(uint dwMessage, ref APPBARDATA pData);

[DllImport("user32.dll")]
private static extern bool GetWindowRect(IntPtr hWnd, out RECT lpRect);

[DllImport("user32.dll")]
private static extern IntPtr MonitorFromWindow(IntPtr hwnd, uint dwFlags);

[DllImport("user32.dll")]
private static extern bool GetMonitorInfo(IntPtr hMonitor, ref MONITORINFO lpmi);

private const uint ABM_GETTASKBARPOS = 0x5u;
private const uint MONITOR_DEFAULTTONEAREST = 2;

private struct RECT { public int Left, Top, Right, Bottom; }
private struct APPBARDATA { public uint cbSize; public IntPtr hWnd; ... public RECT rc; ... }
private struct MONITORINFO { public uint cbSize; public RECT rcMonitor; public RECT rcWork; ... }
```

NOVÉ importy přidat:
```csharp
[DllImport("user32.dll")]
private static extern IntPtr GetForegroundWindow();

private const uint ABM_GETSTATE = 0x4u;
private const int ABS_AUTOHIDE = 0x1;
```

Pole pro nový timer:
```csharp
private DispatcherTimer? _visibilityTimer;
```

Existující Closed event handler (rozšířit o _visibilityTimer?.Stop()):
```csharp
Closed += (_, _) =>
{
    _topMostEnforcer?.Dispose();
    _popup?.Close();
    _refreshTimer?.Stop();
    _textTimer?.Stop();
    // přidat: _visibilityTimer?.Stop();
};
```

Existující pole: `private readonly IntPtr _taskbarHwnd;` — použít pro monitor lookup.
Existující pole: `private readonly bool _isPrimary;` — rozlišení primárního taskbaru.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Přidat P/Invoke importy a StartVisibilityTimer()</name>
  <files>ClaudeUsageWidget/MainWindow.xaml.cs</files>
  <action>
Do MainWindow.xaml.cs přidat:

1. Nové P/Invoke importy a konstanty (za existující importy):
```csharp
[DllImport("user32.dll")]
private static extern IntPtr GetForegroundWindow();

private const uint ABM_GETSTATE = 0x4u;
private const int ABS_AUTOHIDE = 0x1;
```

2. Nové pole (za existující _textTimer):
```csharp
private DispatcherTimer? _visibilityTimer;
```

3. Novou metodu StartVisibilityTimer():
```csharp
private void StartVisibilityTimer()
{
    _visibilityTimer = new DispatcherTimer { Interval = TimeSpan.FromMilliseconds(500) };
    _visibilityTimer.Tick += (_, _) => CheckVisibility();
    _visibilityTimer.Start();
}
```

4. Volání StartVisibilityTimer() v Loaded lambda (za StartTextTimer()):
```csharp
StartTextTimer();
StartVisibilityTimer(); // přidat zde
_ = LoadLatestReleaseAsync();
```

5. Rozšířit Closed event handler o cleanup:
```csharp
_textTimer?.Stop();
_visibilityTimer?.Stop(); // přidat
```
  </action>
  <verify>
    <automated>cd /mnt/c/Users/stepa/source/claude-usage-widget && dotnet build ClaudeUsageWidget/ClaudeUsageWidget.csproj 2>&1 | tail -5</automated>
  </verify>
  <done>Build green. Soubor obsahuje GetForegroundWindow import, ABM_GETSTATE konstantu, _visibilityTimer pole a StartVisibilityTimer() metodu.</done>
</task>

<task type="auto">
  <name>Task 2: Implementovat CheckVisibility(), IsTaskbarVisible(), IsFullscreenOnMyMonitor()</name>
  <files>ClaudeUsageWidget/MainWindow.xaml.cs</files>
  <action>
Do MainWindow.xaml.cs přidat tři metody (za StartVisibilityTimer()):

```csharp
private void CheckVisibility()
{
    bool taskbarVisible = IsTaskbarVisible();
    bool fullscreen = IsFullscreenOnMyMonitor();
    // Locked decision z CONTEXT.md: widget viditelný jen když taskbarVisible && !fullscreenOnSameMonitor
    Visibility = (taskbarVisible && !fullscreen)
        ? Visibility.Visible
        : Visibility.Hidden;
}

private bool IsTaskbarVisible()
{
    if (_isPrimary)
    {
        // Pro primární taskbar: zjistit zda má auto-hide zapnuté
        var stateData = new APPBARDATA { cbSize = (uint)Marshal.SizeOf<APPBARDATA>() };
        uint state = SHAppBarMessage(ABM_GETSTATE, ref stateData);
        bool autoHideEnabled = (state & ABS_AUTOHIDE) != 0;

        if (!autoHideEnabled) return true; // auto-hide vypnutý → vždy viditelný

        // Auto-hide zapnutý: porovnat aktuální pozici taskbaru s expected pozicí
        if (!GetWindowRect(_taskbarHwnd, out RECT actualRect)) return true;
        var posData = new APPBARDATA { cbSize = (uint)Marshal.SizeOf<APPBARDATA>() };
        SHAppBarMessage(ABM_GETTASKBARPOS, ref posData);
        // Taskbar je skrytý = actualRect.Top je za spodní hranicí expected pozice
        return actualRect.Top <= posData.rc.Bottom;
    }
    else
    {
        // Pro sekundární taskbar: heuristika — porovnat Top s dolní hranicí monitoru
        if (!GetWindowRect(_taskbarHwnd, out RECT taskbarRect)) return true;
        var monitor = MonitorFromWindow(_taskbarHwnd, MONITOR_DEFAULTTONEAREST);
        var mi = new MONITORINFO { cbSize = (uint)Marshal.SizeOf<MONITORINFO>() };
        if (!GetMonitorInfo(monitor, ref mi)) return true;
        // Skrytý = Top taskbaru je blízko dolního okraje monitoru (tolerance 2px)
        return taskbarRect.Top < mi.rcMonitor.Bottom - 2;
    }
}

private bool IsFullscreenOnMyMonitor()
{
    var foreground = GetForegroundWindow();
    if (foreground == IntPtr.Zero) return false;

    // Zjistit monitor taskbaru (widget ho sleduje)
    var myMonitor = MonitorFromWindow(_taskbarHwnd, MONITOR_DEFAULTTONEAREST);
    var mi = new MONITORINFO { cbSize = (uint)Marshal.SizeOf<MONITORINFO>() };
    if (!GetMonitorInfo(myMonitor, ref mi)) return false;

    // Zjistit monitor foreground okna
    var fgMonitor = MonitorFromWindow(foreground, MONITOR_DEFAULTTONEAREST);
    if (fgMonitor != myMonitor) return false; // fullscreen na jiném monitoru — neskrývat

    // Porovnat rozměry foreground okna s fyzickými rozměry monitoru (rcMonitor, ne rcWork)
    if (!GetWindowRect(foreground, out RECT fgRect)) return false;
    var mon = mi.rcMonitor;
    return fgRect.Left <= mon.Left && fgRect.Top <= mon.Top
        && fgRect.Right >= mon.Right && fgRect.Bottom >= mon.Bottom;
}
```

POZOR: Používat `_taskbarHwnd` (ne this window handle) pro MonitorFromWindow — takto fungují existující GetTaskbarInfo() a GetMonitorScale() metody v kódu.
POZOR: Fullscreen porovnávat s `rcMonitor` (fyzické rozměry), ne `rcWork` (bez taskbaru).
POZOR: CheckVisibility() je synchronní — všechny volání jsou Win32 P/Invoke. Nepoužívat async.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/stepa/source/claude-usage-widget && dotnet build ClaudeUsageWidget/ClaudeUsageWidget.csproj 2>&1 | tail -5</automated>
  </verify>
  <done>Build green. Soubor obsahuje CheckVisibility(), IsTaskbarVisible() a IsFullscreenOnMyMonitor() metody.</done>
</task>

</tasks>

<verification>
Po dokončení:
- `dotnet build ClaudeUsageWidget/ClaudeUsageWidget.csproj` bez chyb
- Grep: `grep -n "CheckVisibility\|IsTaskbarVisible\|IsFullscreenOnMyMonitor\|_visibilityTimer" ClaudeUsageWidget/MainWindow.xaml.cs`
</verification>

<success_criteria>
- Build green
- _visibilityTimer spuštěn v Loaded, zastaven v Closed
- CheckVisibility() nastavuje Visibility.Visible nebo Visibility.Hidden
- IsTaskbarVisible() rozlišuje primární (_isPrimary) a sekundární taskbar
- IsFullscreenOnMyMonitor() porovnává s _taskbarHwnd monitorem a rcMonitor
</success_criteria>

<output>
Po dokončení vytvořit `.planning/phases/03-data-a-viditelnost/03-02-SUMMARY.md`
</output>
