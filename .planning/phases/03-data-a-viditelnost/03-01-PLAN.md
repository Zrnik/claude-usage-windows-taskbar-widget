---
phase: 03-data-a-viditelnost
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ClaudeUsageWidget/MainWindow.xaml.cs
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04

must_haves:
  truths:
    - "Při selhání API se bary okamžitě vybarví maroon a zobrazí text 'Error'"
    - "Stale data se nikdy nezobrazí po selhání API — _lastUsage je null v error stavu"
    - "Tooltip v error stavu zobrazuje detail chyby (LastError), ne stará data"
    - "Data se obnovují každou minutu"
    - "Credential fallback funguje automaticky (Windows + WSL zdroje)"
  artifacts:
    - path: "ClaudeUsageWidget/MainWindow.xaml.cs"
      provides: "Opravený ShowErrorState() a StartRefreshTimer() Tick handler"
      contains: "Colors.Maroon"
  key_links:
    - from: "MainWindow._refreshTimer.Tick"
      to: "ShowErrorState()"
      via: "unconditional call on null usage"
      pattern: "else\\s+ShowErrorState"
    - from: "ShowErrorState()"
      to: "_lastUsage = null"
      via: "první příkaz metody"
      pattern: "_lastUsage = null"
---

<objective>
Opravit error stav widgetu: maroon barva + "Error" text, okamžitý přechod bez stale dat.

Purpose: Uživatel musí vědět když API selže — aktuálně widget buď zobrazuje stará data (stale data bug) nebo červenou barvu s malým "error". Locked decisions říkají maroon + "Error" (velké E) + null _lastUsage.
Output: Upravený MainWindow.xaml.cs kde ShowErrorState() a refresh timer jsou správné.
</objective>

<execution_context>
@/home/zrny/.claude/get-shit-done/workflows/execute-plan.md
@/home/zrny/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@ClaudeUsageWidget/MainWindow.xaml.cs

<interfaces>
<!-- Relevantní metody a pole z MainWindow.xaml.cs pro tento task -->

Existující ShowErrorState() — NAHRADIT:
```csharp
private void ShowErrorState()
{
    Bar5h.Value = 0;
    Bar7d.Value = 0;
    var red = new SolidColorBrush(Color.FromRgb(0xF4, 0x43, 0x36));
    Text5h.Foreground = red;
    Text7d.Foreground = red;
    Text5h.Text = "error";
    Text7d.Text = "error";
}
```

Existující StartRefreshTimer() Tick handler — OPRAVIT:
```csharp
_refreshTimer.Tick += async (_, _) =>
{
    var usage = await _apiClient.GetUsageAsync();
    if (usage != null)
        UpdateBars(usage);
    else if (_lastUsage == null)   // BUG: condition zabraňuje error při stale datech
        ShowErrorState();
};
```

Existující GetBarIndicator() — použít pro přístup k PART_Indicator:
```csharp
private static Border? GetBarIndicator(ProgressBar bar)
{
    bar.ApplyTemplate();
    return bar.Template.FindName("PART_Indicator", bar) as Border;
}
```

Existující Closed event handler — ROZŠÍŘIT:
```csharp
Closed += (_, _) =>
{
    _topMostEnforcer?.Dispose();
    _popup?.Close();
    _refreshTimer?.Stop();
    _textTimer?.Stop();
};
```

Pole _lastUsage: `private UsageData? _lastUsage;`
ShowPopup() přistupuje k `_apiClient.LastError` pro tooltip — při null _lastUsage zobrazí error detail.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Opravit ShowErrorState() — maroon + "Error" + null _lastUsage</name>
  <files>ClaudeUsageWidget/MainWindow.xaml.cs</files>
  <action>
V MainWindow.xaml.cs nahradit metodu ShowErrorState() novou implementací:

```csharp
private void ShowErrorState()
{
    _lastUsage = null; // okamžitý přechod, bez stale dat — tooltip pak zobrazí LastError
    Bar5h.Value = 100; // Value=100 aby PART_Indicator měl šířku a barva byla viditelná
    Bar7d.Value = 100;
    var maroon = new SolidColorBrush(Colors.Maroon); // #800000 — locked decision z CONTEXT.md
    var ind5h = GetBarIndicator(Bar5h);
    var ind7d = GetBarIndicator(Bar7d);
    if (ind5h != null) ind5h.Background = maroon;
    if (ind7d != null) ind7d.Background = maroon;
    Text5h.Foreground = Brushes.White;
    Text7d.Foreground = Brushes.White;
    Text5h.Text = "Error"; // velké E — locked decision z CONTEXT.md
    Text7d.Text = "Error";
}
```

POZOR: `Bar5h.Value = 100` (ne 0) — pokud je Value=0, PART_Indicator má šířku 0 a maroon barva je neviditelná.
POZOR: `_lastUsage = null` musí být první příkaz — zajistí že ShowPopup() použije LastError místo stale dat.
POZOR: Používat `Colors.Maroon` (System.Windows.Media) ne hex string — konzistentní s locked decision.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/stepa/source/claude-usage-widget && dotnet build ClaudeUsageWidget/ClaudeUsageWidget.csproj 2>&1 | tail -5</automated>
  </verify>
  <done>Build green. ShowErrorState() obsahuje _lastUsage = null, Colors.Maroon, Value=100, Text="Error".</done>
</task>

<task type="auto">
  <name>Task 2: Opravit StartRefreshTimer Tick — okamžitý error bez stale dat</name>
  <files>ClaudeUsageWidget/MainWindow.xaml.cs</files>
  <action>
V StartRefreshTimer() opravit Tick handler — odstranit podmínku `else if (_lastUsage == null)`:

PŘED (bug):
```csharp
if (usage != null)
    UpdateBars(usage);
else if (_lastUsage == null)
    ShowErrorState();
```

PO (správně):
```csharp
if (usage != null)
    UpdateBars(usage);
else
    ShowErrorState(); // vždy při selhání — bez podmínky; locked decision z CONTEXT.md
```

Důvod: Podmínka `_lastUsage == null` způsobovala zobrazování stale dat při opakovaném selhání API. Nová ShowErrorState() nastavuje `_lastUsage = null` takže je bezpečné volat ji vždy.

Credential fallback (DATA-02): CredentialStore.LoadAllCredentials() + ClaudeApiClient.SetCredentials() jsou již volány v Program.cs OnStartup() — žádná změna potřeba.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/stepa/source/claude-usage-widget && dotnet build ClaudeUsageWidget/ClaudeUsageWidget.csproj 2>&1 | tail -5</automated>
  </verify>
  <done>Build green. Tick handler obsahuje unconditional `else ShowErrorState()` bez podmínky.</done>
</task>

</tasks>

<verification>
Po dokončení obou tasků:
- `dotnet build ClaudeUsageWidget/ClaudeUsageWidget.csproj` bez chyb
- Grep ověření: `grep -n "Colors.Maroon\|_lastUsage = null\|else\s*$" ClaudeUsageWidget/MainWindow.xaml.cs`
</verification>

<success_criteria>
- Build green
- ShowErrorState() nastavuje _lastUsage = null jako první příkaz
- ShowErrorState() používá Colors.Maroon a Bar.Value=100
- ShowErrorState() nastavuje Text5h.Text = "Error" (velké E)
- StartRefreshTimer Tick má unconditional else ShowErrorState()
</success_criteria>

<output>
Po dokončení vytvořit `.planning/phases/03-data-a-viditelnost/03-01-SUMMARY.md`
</output>
