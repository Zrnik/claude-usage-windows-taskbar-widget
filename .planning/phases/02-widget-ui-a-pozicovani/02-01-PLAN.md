---
phase: 02-widget-ui-a-pozicovani
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ClaudeUsageWidget/ClaudeUsageWidgetProvider/MainWindow.xaml
  - ClaudeUsageWidget/ClaudeUsageWidgetProvider/MainWindow.xaml.cs
  - ClaudeUsageWidget/ClaudeUsageWidgetProvider/Program.cs
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04, UI-05, LIFE-01]

must_haves:
  truths:
    - "Aplikace se spustí bez okna v taskbaru a bez splash screenu"
    - "Widget zobrazí borderless okno výšky taskbaru (48px) bez titulbaru"
    - "Jsou viditelné dva progress bary nad sebou — horní 5h, dolní 7d"
    - "Barva baru se mění dle hodnoty: zelená < 75%, oranžová 75-90%, červená >= 90%"
    - "Text s procentem je zobrazen uvnitř každého baru"
  artifacts:
    - path: "ClaudeUsageWidget/ClaudeUsageWidgetProvider/MainWindow.xaml"
      provides: "WPF hlavní okno — borderless, výška taskbaru, 2 progress bary"
      contains: "WindowStyle=None ShowInTaskbar=False"
    - path: "ClaudeUsageWidget/ClaudeUsageWidgetProvider/MainWindow.xaml.cs"
      provides: "Code-behind — inicializace okna, nastavení výšky, placeholder data"
      exports: ["MainWindow"]
    - path: "ClaudeUsageWidget/ClaudeUsageWidgetProvider/Program.cs"
      provides: "App startup — vytvoří MainWindow jako hlavní okno"
  key_links:
    - from: "Program.cs App.OnStartup"
      to: "MainWindow"
      via: "new MainWindow().Show()"
      pattern: "new MainWindow"
    - from: "MainWindow.xaml ProgressBar"
      to: "ValueConverter nebo code-behind"
      via: "barva dle utilization hodnoty"
      pattern: "Background.*Converter|SetBarColor"
---

<objective>
Vytvořit hlavní WPF okno widgetu s progress bary a nastavit správný vzhled (borderless, bez titulbaru, výška taskbaru).

Purpose: Viditelný základ widgetu — bez tohoto kroku nelze testovat pozicování ani data.
Output: MainWindow.xaml + code-behind, upravený Program.cs spouštějící okno.
</objective>

<execution_context>
@/home/zrny/.claude/get-shit-done/workflows/execute-plan.md
@/home/zrny/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-widget-ui-a-pozicovani/02-CONTEXT.md

<interfaces>
<!-- Existující kód — executor nepotřebuje prohledávat codebase -->

Z Program.cs:
```csharp
internal class App : Application
{
    [STAThread]
    static void Main(string[] args)
    {
        var app = new App();
        app.Run();
    }
}

internal static class TimeFormatter
{
    internal static string FormatResetTime(DateTimeOffset resetTime);
    // vrací: "in 2h 30m", "in 45m", "now"
}
```

Z ClaudeApiClient.cs:
```csharp
internal sealed class UsageData
{
    public double FiveHourUtilization { get; set; }   // 0-100 (procenta)
    public DateTimeOffset FiveHourResetsAt { get; set; }
    public double SevenDayUtilization { get; set; }   // 0-100 (procenta)
    public DateTimeOffset SevenDayResetsAt { get; set; }
}
```

Namespace: `ClaudeUsageWidgetProvider`, target: `net8.0-windows10.0.22621.0`, UseWPF=true
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vytvořit MainWindow.xaml s progress bary</name>
  <files>
    ClaudeUsageWidget/ClaudeUsageWidgetProvider/MainWindow.xaml
    ClaudeUsageWidget/ClaudeUsageWidgetProvider/MainWindow.xaml.cs
  </files>
  <action>
Vytvořit MainWindow.xaml s těmito vlastnostmi okna:
- `WindowStyle="None"` — bez titulbaru a rámečku
- `ShowInTaskbar="False"` — nezobrazovat v taskbaru
- `ResizeMode="NoResize"`
- `Topmost="True"` — always-on-top (POS-03)
- `Background="#1C1C1C"` — splývá s Win11 dark taskbarem
- `AllowsTransparency="False"` — není potřeba, barva postačí
- Výška: 48 (placeholder, code-behind přepíše na skutečnou výšku taskbaru)
- Šířka: 120 (placeholder, code-behind může upravit)

Layout XAML — Grid se dvěma řádky pro progress bary:
```xml
<Grid Margin="4,6,4,6">
    <Grid.RowDefinitions>
        <RowDefinition Height="*"/>
        <RowDefinition Height="6"/>
        <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <!-- Bar 5h -->
    <Grid Grid.Row="0">
        <ProgressBar x:Name="Bar5h" Minimum="0" Maximum="100" Value="0"/>
        <TextBlock x:Name="Text5h" HorizontalAlignment="Center" VerticalAlignment="Center"
                   Foreground="White" FontSize="9" Text="0%"/>
    </Grid>
    <!-- Bar 7d -->
    <Grid Grid.Row="2">
        <ProgressBar x:Name="Bar7d" Minimum="0" Maximum="100" Value="0"/>
        <TextBlock x:Name="Text7d" HorizontalAlignment="Center" VerticalAlignment="Center"
                   Foreground="White" FontSize="9" Text="0%"/>
    </Grid>
</Grid>
```

Barvy progress barů — odstranit default WPF styl a použít flat vzhled. V App.xaml nebo přímo v MainWindow.xaml přepsat template ProgressBaru na jednoduchý Rectangle bez border-radius:
```xml
<ProgressBar.Template>
    <ControlTemplate TargetType="ProgressBar">
        <Grid>
            <Border Background="#333333"/>
            <Border x:Name="PART_Track"/>
            <Border x:Name="PART_Indicator" HorizontalAlignment="Left" Background="Green"/>
        </Grid>
    </ControlTemplate>
</ProgressBar.Template>
```

V MainWindow.xaml.cs implementovat:
1. `UpdateBars(UsageData data)` — nastaví Value, Text a barvu pro oba bary
2. Logika barev: `utilization < 75` → `#4CAF50` (zelená), `< 90` → `#FF9800` (oranžová), `>= 90` → `#F44336` (červená)
3. Helper `SetBarColor(Border indicator, double utilization)` — nastaví Background dle prahu
4. V konstruktoru: zavolat `SetTaskbarHeight()` který přečte výšku systémového taskbaru přes `SystemParameters.PrimaryScreenHeight - SystemParameters.WorkArea.Height` a nastaví `this.Height`
5. Placeholder data v konstruktoru pro vizuální test: `UpdateBars(new UsageData { FiveHourUtilization = 50, SevenDayUtilization = 80 })`

Metoda `UpdateBars` musí být `public` — bude volána z Plan 03 po načtení API dat.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/stepa/source/claude-usage-widget/ClaudeUsageWidget && dotnet build ClaudeUsageWidgetProvider/ClaudeUsageWidgetProvider.csproj 2>&1 | tail -5</automated>
  </verify>
  <done>
    - dotnet build proběhne bez chyb
    - MainWindow.xaml a MainWindow.xaml.cs existují
    - Metoda UpdateBars(UsageData) je public
    - SetTaskbarHeight() v konstruktoru nastaví výšku okna
  </done>
</task>

<task type="auto">
  <name>Task 2: Upravit Program.cs — spustit MainWindow jako hlavní okno</name>
  <files>
    ClaudeUsageWidget/ClaudeUsageWidgetProvider/Program.cs
  </files>
  <action>
Upravit `App` třídu v Program.cs — přepsat `OnStartup` tak, aby:
1. Načetl credentials přes `CredentialStore.LoadCredential()`
2. Pokud credentials nenalezeny: `MessageBox.Show("Credentials not found.", "Claude Usage Widget"); Shutdown(); return;`
3. Vytvořil a zobrazil `MainWindow`: `var window = new MainWindow(); MainWindow = window; window.Show();`
4. Nastavit `ShutdownMode = ShutdownMode.OnMainWindowClose`

Přidat override `OnStartup(StartupEventArgs e)` — odstranit prázdné `app.Run()` volání nebo ho zachovat, ale přidat `StartupUri` nebo přepsat `OnStartup`.

Nejjednodušší přístup: přidat `App.xaml` není nutné — stačí přidat override `OnStartup` přímo do třídy `App` v Program.cs:

```csharp
protected override void OnStartup(StartupEventArgs e)
{
    base.OnStartup(e);
    ShutdownMode = ShutdownMode.OnMainWindowClose;

    var credential = CredentialStore.LoadCredential();
    if (credential == null)
    {
        MessageBox.Show("Claude credentials not found.\nExpected: ~/.claude/.credentials.json",
            "Claude Usage Widget", MessageBoxButton.OK, MessageBoxImage.Error);
        Shutdown();
        return;
    }

    var window = new MainWindow();
    MainWindow = window;
    window.Show();
}
```

`TimeFormatter` třída zůstává beze změny — bude použita v Plan 03.

POZNÁMKA: Main() metoda musí volat `app.Run()` (ne jen `new App()`), aby WPF message loop běžel.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/stepa/source/claude-usage-widget/ClaudeUsageWidget && dotnet build ClaudeUsageWidgetProvider/ClaudeUsageWidgetProvider.csproj 2>&1 | tail -5</automated>
  </verify>
  <done>
    - dotnet build proběhne bez chyb
    - App.OnStartup načítá credentials a zobrazuje MainWindow
    - Bez credentials zobrazí chybovou hlášku a ukončí se
  </done>
</task>

</tasks>

<verification>
Po dokončení obou tasků:
```
cd /mnt/c/Users/stepa/source/claude-usage-widget/ClaudeUsageWidget
dotnet build ClaudeUsageWidgetProvider/ClaudeUsageWidgetProvider.csproj
```
Build musí proběhnout čistě (0 errors, 0 warnings nebo jen warnings bez chyb).
</verification>

<success_criteria>
- dotnet build: 0 errors
- MainWindow.xaml existuje s WindowStyle=None, ShowInTaskbar=False, Topmost=True
- Dva progress bary s flat template (bez border-radius), text uvnitř
- UpdateBars(UsageData) je public metoda
- Program.cs spouští MainWindow přes OnStartup override
</success_criteria>

<output>
Po dokončení vytvořit `.planning/phases/02-widget-ui-a-pozicovani/02-01-SUMMARY.md`
</output>
