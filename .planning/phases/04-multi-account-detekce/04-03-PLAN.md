---
phase: 04-multi-account-detekce
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - ClaudeUsageWidget/Program.cs
autonomous: false
requirements:
  - MULTI-01
  - MULTI-02
  - MULTI-03
must_haves:
  truths:
    - "Každý unikátní účet (org/token) dostane vlastní MainWindow s vlastním ClaudeApiClient"
    - "Pokud žádné credentials neexistují, widget se zobrazí s error stavem (necrashne)"
    - "Claude účty se zobrazí před Codex účty (pořadí z LoadAllAccounts)"
    - "Chybějící credentials pro Codex nebrání zobrazení Claude barů"
  artifacts:
    - path: "ClaudeUsageWidget/Program.cs"
      provides: "Per-account MainWindow instantiation v OnStartup"
      contains: "CredentialStore.LoadAllAccounts()"
  key_links:
    - from: "OnStartup"
      to: "CredentialStore.LoadAllAccounts()"
      via: "foreach — každý AccountInfo → ClaudeApiClient(account) → MainWindow"
      pattern: "LoadAllAccounts"
    - from: "accounts.Count == 0"
      to: "fallback AccountInfo"
      via: "prázdný OAuthCredential, ServiceType.Claude, label = no-credentials"
      pattern: "no-credentials"
---

<objective>
Přepojit Program.cs na per-account MainWindow instantiation.

Purpose: Místo jednoho sdíleného ClaudeApiClient pro všechna okna vytvoří OnStartup jeden ClaudeApiClient per AccountInfo. Stávající logika pro primární taskbar a sekundární taskbary (EnumWindows) bude zachována — pro Phase 5 bude multi-account UI; zatím každý účet sdílí stejnou sadu taskbarů.
Output: Program.cs vytváří per-account ClaudeApiClient instance. Widget zobrazuje jedno okno per taskbar per účet (Phase 5 změní layout).
</objective>

<execution_context>
@/home/zrny/.claude/get-shit-done/workflows/execute-plan.md
@/home/zrny/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-multi-account-detekce/04-01-SUMMARY.md
@.planning/phases/04-multi-account-detekce/04-02-SUMMARY.md

<interfaces>
<!-- Z Plan 01 (04-01) -->
```csharp
public static List<AccountInfo> LoadAllAccounts(); // returns Claude + Codex, deduplicated

internal enum ServiceType { Claude, Codex }
internal sealed record AccountInfo(ServiceType Service, OAuthCredential Credential, string Label);
```

<!-- Z Plan 02 (04-02) -->
```csharp
internal ClaudeApiClient(AccountInfo account); // per-account, _noReload = true
internal ClaudeApiClient();                    // default — stále funkční
```

<!-- Stávající Program.cs — OnStartup (relevantní část) -->
```csharp
protected override void OnStartup(StartupEventArgs e)
{
    base.OnStartup(e);
    ShutdownMode = ShutdownMode.OnExplicitShutdown;

    var apiClient = new ClaudeApiClient();        // <- nahradit per-account logikou

    Exit += (_, _) => apiClient.Dispose();

    var primaryHwnd = FindWindow("Shell_TrayWnd", null);
    var primaryWindow = new MainWindow(apiClient, primaryHwnd, isPrimary: true);
    MainWindow = primaryWindow;
    primaryWindow.Show();

    EnumWindows((hwnd, _) =>
    {
        var sb = new StringBuilder(64);
        GetClassName(hwnd, sb, 64);
        if (sb.ToString() == "Shell_SecondaryTrayWnd")
            new MainWindow(apiClient, hwnd, isPrimary: false).Show();
        return true;
    }, IntPtr.Zero);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Přepsat OnStartup pro per-account instantiation</name>
  <files>ClaudeUsageWidget/Program.cs</files>
  <action>
Nahradit obsah OnStartup (od `var apiClient` níže) touto logikou:

```csharp
base.OnStartup(e);
ShutdownMode = ShutdownMode.OnExplicitShutdown;

// Načíst všechny unikátní účty (Claude Windows+WSL deduplikováno, Codex)
var accounts = CredentialStore.LoadAllAccounts();

// Fallback: žádné credentials → jeden client v error stavu
if (accounts.Count == 0)
    accounts.Add(new AccountInfo(ServiceType.Claude, new OAuthCredential(), "no-credentials"));

var clients = accounts.Select(a => new ClaudeApiClient(a)).ToList();

Exit += (_, _) =>
{
    foreach (var c in clients) c.Dispose();
};

var primaryHwnd = FindWindow("Shell_TrayWnd", null);

// Primární okno = první účet
var primaryWindow = new MainWindow(clients[0], primaryHwnd, isPrimary: true);
MainWindow = primaryWindow;
primaryWindow.Show();

// Pro každý sekundární taskbar: zobrazit první účet
// (Phase 5 změní layout na horizontální řadu per-taskbar)
EnumWindows((hwnd, _) =>
{
    var sb = new StringBuilder(64);
    GetClassName(hwnd, sb, 64);
    if (sb.ToString() == "Shell_SecondaryTrayWnd")
        new MainWindow(clients[0], hwnd, isPrimary: false).Show();
    return true;
}, IntPtr.Zero);
```

Přidat `using System.Linq;` na začátek souboru pokud ještě není (nebo je v GlobalUsings.cs — pak nepřidávat).

Poznámka: Phase 5 přidá horizontální layout pro více účtů. Prozatím widget zobrazuje první účet — architektura je připravena (clients list existuje).
  </action>
  <verify>
    <automated>dotnet build /mnt/c/Users/stepa/source/claude-usage-widget/ClaudeUsageWidget/ClaudeUsageWidget.csproj</automated>
  </verify>
  <done>Build projde. Program.cs používá CredentialStore.LoadAllAccounts() a per-account ClaudeApiClient(AccountInfo). Fallback pro prázdný seznam existuje.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Ověřit funkčnost widgetu po multi-account refaktoru</name>
  <action>Manuální ověření widgetu po implementaci per-account loading.</action>
  <files></files>
  <verify>
    <automated>MISSING — manuální checkpoint</automated>
  </verify>
  <done>Widget se zobrazí, načte data, crash log je čistý.</done>
  <what-built>
    Multi-account credential loading: CredentialStore načítá Claude (Windows + WSL) a Codex credentials, deduplikuje je, a Program.cs vytváří per-account API klienty. Widget by měl fungovat identicky jako před Phase 4 pro single-account uživatele.
  </what-built>
  <how-to-verify>
    1. Sestavit a spustit widget: `dotnet run --project ClaudeUsageWidget/ClaudeUsageWidget.csproj` nebo spustit exe z bin/
    2. Ověřit, že widget se zobrazí v taskbaru (stejné chování jako před Phase 4)
    3. Ověřit, že usage bary zobrazují data (Claude rate limits se načtou)
    4. Zkontrolovat crash log (`ClaudeUsageWidget.log` vedle exe) — nesmí být žádná exception
    5. (Volitelné) Pokud máš WSL Claude credentials: ověřit, že se nezobrazí duplicitní okno pro stejný token
    6. (Volitelné) Pokud máš `~/.codex/auth.json`: ověřit, že Codex credentials jsou načteny bez chyby (log)
  </how-to-verify>
  <resume-signal>Napiš "ok" nebo popiš problémy</resume-signal>
</task>

</tasks>

<verification>
dotnet build + manuální test s reálnými credentials.
</verification>

<success_criteria>
- Build projde bez chyb
- Widget se zobrazí a načte Claude usage data (identicky jako v Phase 3)
- Žádné neočekávané výjimky v crash logu
- LoadAllAccounts() je voláno v OnStartup
- Per-account ClaudeApiClient(AccountInfo) je instancován
</success_criteria>

<output>
Po dokončení vytvoř `.planning/phases/04-multi-account-detekce/04-03-SUMMARY.md`
</output>
