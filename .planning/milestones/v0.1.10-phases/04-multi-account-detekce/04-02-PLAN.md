---
phase: 04-multi-account-detekce
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ClaudeUsageWidget/ClaudeApiClient.cs
autonomous: true
requirements:
  - MULTI-03
must_haves:
  truths:
    - "ClaudeApiClient lze instanciovat s konkrétním AccountInfo — nepoužívá rotaci credentials"
    - "Per-account instance nikdy nepřepisuje svůj credential voláním LoadAllCredentials()"
    - "Původní bezparametrový konstruktor stále funguje pro fallback path v Program.cs"
  artifacts:
    - path: "ClaudeUsageWidget/ClaudeApiClient.cs"
      provides: "Per-account konstruktor ClaudeApiClient(AccountInfo)"
      contains: "_noReload"
  key_links:
    - from: "ClaudeApiClient(AccountInfo account)"
      to: "_credentials = [account.Credential]"
      via: "single-item list, _noReload = true"
      pattern: "_noReload"
    - from: "GetUsageAsync"
      to: "CredentialStore.LoadAllCredentials()"
      via: "pouze pokud !_noReload"
      pattern: "if.*_credentialIndex == 0 && !_noReload"
---

<objective>
Přidat per-account konstruktor do ClaudeApiClient a opravit credential reload bug.

Purpose: Stávající GetUsageAsync() přepisuje credentials při každém volání (`_credentials = CredentialStore.LoadAllCredentials()`). Per-account instance by tím ztratila svůj fixed credential. Flag `_noReload` tento reload podmíní.
Output: ClaudeApiClient s novým konstruktorem `ClaudeApiClient(AccountInfo)` a opravou reload logiky.
</objective>

<execution_context>
@/home/zrny/.claude/get-shit-done/workflows/execute-plan.md
@/home/zrny/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Typy z Plan 01 (04-01-PLAN.md) — musí existovat před spuštěním tohoto plánu -->
```csharp
internal enum ServiceType { Claude, Codex }
internal sealed record AccountInfo(ServiceType Service, OAuthCredential Credential, string Label);
```

<!-- Stávající ClaudeApiClient — relevantní části -->
```csharp
internal sealed class ClaudeApiClient : IDisposable
{
    private List<OAuthCredential> _credentials = [];
    private int _credentialIndex;
    private OAuthCredential? _credential => _credentialIndex < _credentials.Count ? _credentials[_credentialIndex] : null;

    // Problematický řádek v GetUsageAsync (line 49-50):
    if (_credentialIndex == 0)
        _credentials = CredentialStore.LoadAllCredentials(); // přepisuje per-account credential!

    // Existing constructor: implicit default (no-arg)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Přidat per-account konstruktor a _noReload flag do ClaudeApiClient</name>
  <files>ClaudeUsageWidget/ClaudeApiClient.cs</files>
  <action>
Do ClaudeApiClient.cs provést dvě změny:

**Změna 1 — přidat `_noReload` field a per-account konstruktor:**

Přidat privátní field (vedle existujících):
```csharp
private bool _noReload;
```

Přidat nový internal konstruktor za existující field deklarace (před GetUsageAsync):
```csharp
internal ClaudeApiClient(AccountInfo account)
{
    _credentials = [account.Credential];
    _credentialIndex = 0;
    _noReload = true;
}
```

Bezparametrový (default) konstruktor nevytvářet explicitně — C# ho generuje automaticky, stávající kód funguje dál.

**Změna 2 — opravit credential reload v GetUsageAsync:**

Stávající kód (řádky 49-50):
```csharp
if (_credentialIndex == 0)
    _credentials = CredentialStore.LoadAllCredentials();
```

Nahradit:
```csharp
if (_credentialIndex == 0 && !_noReload)
    _credentials = CredentialStore.LoadAllCredentials();
```

Žádné jiné změny v souboru.
  </action>
  <verify>
    <automated>dotnet build /mnt/c/Users/stepa/source/claude-usage-widget/ClaudeUsageWidget/ClaudeUsageWidget.csproj</automated>
  </verify>
  <done>Build projde. ClaudeApiClient má konstruktor přijímající AccountInfo. Při `_noReload = true` GetUsageAsync nevola LoadAllCredentials(). Stávající `new ClaudeApiClient()` (bezparametrový) stále funguje.</done>
</task>

</tasks>

<verification>
dotnet build ClaudeUsageWidget/ClaudeUsageWidget.csproj — musí projít bez chyb.
</verification>

<success_criteria>
- ClaudeApiClient.cs kompiluje
- Nový konstruktor ClaudeApiClient(AccountInfo) je přítomný
- `_noReload` field existuje
- GetUsageAsync kontroluje `!_noReload` před reloadem z disku
- Bezparametrový konstruktor (stávající Program.cs) stále funguje
</success_criteria>

<output>
Po dokončení vytvoř `.planning/phases/04-multi-account-detekce/04-02-SUMMARY.md`
</output>
